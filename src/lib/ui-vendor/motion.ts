/**
 * Motion tokens vendored for the FAQ accordion (the only consumer in this
 * project). Originally a full copy of the kit's motion set; trimmed
 * 2026-06-09 to the single token actually used -- EASE_OUT. See
 * TECH_AUDIT.md. If a future vendored component needs more, re-import from
 * the kit rather than regrowing this file.
 */

export const EASE_OUT = [0.16, 1, 0.3, 1] as const;
